package com.example.customnpcmod;

import net.minecraft.client.render.entity.state.LivingEntityRenderState;

public class CustomVillagerRenderState extends LivingEntityRenderState {
    // This render state contains all the animation and rendering data
    // for our custom villager entity, extending the base living entity state
    // which includes bodyYaw, pitch, deathTime, limbSwing, etc.
}
